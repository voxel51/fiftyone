# Dependencies for the e2e suite (e2e-pw) beyond fiftyone's own installs.
# The unit-test stack in github.txt (torch, tensorflow, pytest, ...) is not
# needed here and would triple the size of the prebaked e2e image.

# embeddings specs (fiftyone.brain compute_visualization)
umap-learn>=0.5.12
numpy<3

# 3D specs (PCD generation in the media factory, fiftyone.utils.utils3d)
open3d>=0.19.0
