# AGENTS.md

Instructions for AI coding agents working in this repository. FiftyOne is a
Python core (`fiftyone/`) plus a React App (`app/`).

- Python / core work: see `STYLE_GUIDE.md`.
- App / UI work: **read `app/AGENTS.md` before writing code.** App UI is built
  with VOODO (`@voxel51/voodo`), not Material UI, and `app/CODING_STANDARDS.md`
  is binding.
