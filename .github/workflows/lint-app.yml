name: Lint App

on: workflow_call

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            changes:
              - 'app/**'

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Enable corepack
        run: corepack enable
      - name: Prepare yarn 4
        run: COREPACK_NPM_REGISTRY=https://registry.npmjs.org corepack prepare yarn@4.17.1 --activate

      - name: Cache Node Modules
        id: node-cache
        uses: actions/cache@v5
        with:
          path: |
            app/node_modules
            app/.yarn/cache
          key: node-modules-${{ hashFiles('app/yarn.lock') }}

      - name: Install Dependencies
        run: cd app && yarn install

      - parallel:
          - name: Lint ESLint packages
            run: |
              cd app
              ESLINT_PACKAGES=$(grep -v '^#' ./eslint-packages.txt | xargs)
              yarn eslint $ESLINT_PACKAGES

          - name: Check formatting
            run: |
              cd app
              yarn format:check

          - name: Check multimodal package
            run: |
              cd app
              yarn check
