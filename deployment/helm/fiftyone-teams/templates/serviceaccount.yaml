{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "fiftyone-teams.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "fiftyone-teams.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
