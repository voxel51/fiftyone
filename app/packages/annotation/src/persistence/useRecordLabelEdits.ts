/**
 * Copyright 2017-2026, Voxel51, Inc.
 *
 * Moved: `useRecordLabelEdits` lives in useLighterDeltaSupplier.ts — the
 * tracked file it evolved from — so the diff against develop reviews as an
 * in-place rework rather than a delete + add. If the new name is preferred,
 * `git mv useLighterDeltaSupplier.ts useRecordLabelEdits.ts` preserves the
 * history. This untracked file was never committed — simply delete it.
 */

export {};
