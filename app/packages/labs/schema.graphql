type Mutation {
  login(user: UserInput!): User!
}

type Query {
  users: [User!]!
  viewer: User!
}

type User {
  id: ID!
}

input UserInput {
  email: String!
  familyName: String!
  givenName: String!
  sub: String!
}
