# media-2d

Media 2D owns presentation machinery shared by still images and video frames:
textures, fitting, pan and zoom, annotations, and two-dimensional render
surfaces.

It consumes prepared IR and exposes reusable rendering behavior. Stream choice,
playback scheduling, tile composition, and persisted product settings remain in
views.
