# observability

Format-neutral, headless observation contracts live here. Product and runtime
surfaces publish neutral events; adapters may install sinks that interpret them
for a concrete format. This namespace never imports product composition,
renderers, adapters, or React.
