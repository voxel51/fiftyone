# utils

Utils is for small algorithms with no honest owner.

Code here should be pure, broadly reusable, and independent of React, I/O, and
format state. The bar is intentionally high: a helper used by one subsystem is
usually easier to understand beside that subsystem.

Treat this namespace as a last resort, not a waiting room. A growing cluster of
related helpers is evidence that a real domain wants to exist.
